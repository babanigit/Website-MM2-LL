import { FilterOtherSwitchersPipe } from './filter-other-switchers.pipe';

describe('FilterOtherSwitchersPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOtherSwitchersPipe();
    expect(pipe).toBeTruthy();
  });
});
